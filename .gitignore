# Working planning doc - pick-list for the 1.0 release, not for the repo
ROADMAP_TO_1.0.md

# Python
backend/.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
build/
dist/

# Node / Vite
frontend/node_modules/
frontend/dist/
frontend/.vite/

# Rust / Tauri build artifacts (huge - never commit; regenerated by `tauri build`)
frontend/src-tauri/target/
**/*.rlib
**/*.pdb

# Local data (DB + encrypted secret store) - never commit
*.db
*.db-wal
*.db-shm
secrets.enc

# Env / secrets
.env
backend/.env

# OS
.DS_Store
Thumbs.db

# macOS sidecar - regenerated locally by dev-app.sh / build-desktop.sh (34 MB)
frontend/src-tauri/binaries/raplmail-backend-aarch64-apple-darwin

# Tauri updater signing private key - NEVER commit this
frontend/src-tauri/.tauri-updater.key
frontend/src-tauri/.tauri-updater.key.pub
test-samples/